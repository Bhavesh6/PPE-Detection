{
  "name": "safetyfirst-frontend-devtools",
  "private": true,
  "version": "0.0.0",
  "description": "Dev-only tooling for this frontend. NOT part of the deployed app: production still serves these HTML/CSS/JS files exactly as-is, unbuilt, the way backend/app.py and Dockerfile already do. This exists solely to host Reticle (https://reticle.sh) during local development.",
  "type": "module",
  "scripts": {
    "verify": "vite --port 5173 --strictPort"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "@reticlehq/vite-plugin": "^2.12.0",
    "@reticlehq/browser": "^2.12.0"
  }
}
